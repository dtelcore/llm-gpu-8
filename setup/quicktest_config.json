{
  "model": {
    "name": "Tiny (Quick Test)",
    "vocab_size": null,
    "max_len": 16,
    "embedding_dim": 32,
    "num_heads": 4,
    "num_layers": 2,
    "dropout_prob": 0.0,
    "init_scale": 0.02,
    "description": "Tiny model for a fast end-to-end train->generate smoke test."
  },
  "dataset": {
    "name": "tiny_english",
    "corpus": [
      "the quick brown fox jumps over the lazy dog",
      "machine learning trains models on large datasets",
      "artificial intelligence mimics human intelligence",
      "neural networks learn patterns from data",
      "deep learning uses multiple layers for feature extraction"
    ],
    "vocab_size": 27
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Quick Test",
    "learning_rate": 0.01,
    "weight_decay": 0.0,
    "batch_size": 4,
    "num_epochs": 20,
    "warmup_steps": 5,
    "gradient_clip": 1.0,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "quick-test"
  }
}
